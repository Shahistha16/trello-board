import * as React from "react";
import AddCard from "./index";
import { strings } from "../../settings/strings";

const AddCardCosmos = () => {
  const onChange = () => {};
  return (
    <div>
      <AddCard onChange={onChange}></AddCard>
    </div>
  );
};

export default AddCardCosmos;
